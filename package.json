{
  "name": "courier-management-system",
  "version": "1.0.0",
  "description": "Courier Management System built with MERN Stack",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-server": "cd backend && npm install",
    "install-client": "cd frontend && npm install", 
    "install-all": "npm run install-server && npm run install-client",
    "kill-ports": "npx kill-port 3000 5000 || echo 'Ports cleaned'",
    "start": "echo '🚀 Starting CMS Application...' && npm run dev",
    "setup": "npm run install-all && npm run init-db && echo Setup complete! Run 'npm run dev' to start all services.",
    "init-db": "cd backend && node scripts/initDB.js",
    "check-db": "cd backend && node scripts/checkDB.js"
  },
  "author": "Developer",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "kill-port": "^2.0.1"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}